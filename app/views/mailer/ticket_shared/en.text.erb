Ticket shared: #{ticket.title}

Hello #{recipient.firstname},

#{shared_by.firstname} #{shared_by.lastname} has shared the ticket "#{ticket.title}" with you.

You can now view and collaborate on this ticket through the Zammad platform.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}
