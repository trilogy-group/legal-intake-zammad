Ticket closed: #{ticket.title}

Hello #{recipient.firstname},

Your ticket "#{ticket.title}" has been closed.

You can still view the ticket details and history on the Zammad platform.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}
