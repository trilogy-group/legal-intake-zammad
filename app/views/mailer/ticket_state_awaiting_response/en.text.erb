Awaiting Response: #{ticket.title}

Hello #{recipient.firstname},

The legal team needs additional information regarding your ticket "#{ticket.title}" before they can proceed.

<% if article && article.body.present? %>
Latest Comment:
#{article.body_as_text}

<% end %>
Please review the comment above and provide the requested information. The legal team will be able to continue processing your request once they receive your response.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}
