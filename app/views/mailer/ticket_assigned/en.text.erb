Ticket assigned: #{ticket.title}

Hello #{recipient.firstname},

<% if current_user %>
#{current_user.firstname} #{current_user.lastname} has assigned the request "#{ticket.title}" to you.
<% else %>
The request "#{ticket.title}" has been assigned to you.
<% end %>

You can now review and work on this request through the Legal Intake platform.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}
