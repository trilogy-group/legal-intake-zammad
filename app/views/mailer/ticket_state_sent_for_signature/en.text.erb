Sent for Signature: #{ticket.title}

Hello #{recipient.firstname},

Your ticket "#{ticket.title}" has been sent for signature by #{current_user.firstname} #{current_user.lastname}.

The documents are currently in the signature process. You will be notified once all signatures have been completed.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}

You are receiving this because you are a member of the group of this ticket.
