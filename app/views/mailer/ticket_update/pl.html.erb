Aktualizacja zgłoszenia (#{ticket.title})

<div>Cześć #{recipient.firstname},</div>
<br>
<div>
Zgłoszenie (#{ticket.title}) zostało zaktualizowane przez "<b>#{current_user.longname}</b>".
</div>
<br>
<% if @objects[:changes].present? %>
  <div>
  Zmiany:<br>
  <% @objects[:changes].each do |key, value| %>
    <%= t key %>: <%= h value[0] %> -&gt; <%= h value[1] %><br>
  <% end %>
  </div>
<% end %>
<br>

<br>
<div>
  <a href="#{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}" target="zammad_app">Zobacz w #{config.product_name}</a>
</div>
