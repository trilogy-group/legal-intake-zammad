New comment: #{ticket.title}

Hello #{recipient.firstname},

A new comment has been added to your ticket "#{ticket.title}".

--- Comment ---
#{article.created_by.firstname} #{article.created_by.lastname} commented:
#{article.body_as_text}
---------------

You're receiving this notification because #{reason}. Join the discussion to stay updated on the latest developments.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}
