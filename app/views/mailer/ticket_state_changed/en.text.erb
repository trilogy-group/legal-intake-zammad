Ticket status updated: #{ticket.title}

Hello #{recipient.firstname},

The status of your ticket "#{ticket.title}" has been updated to #{ticket.state.name}.

You can view the latest details and any updates on the ticket.

View Ticket: #{config.http_type}://#{config.fqdn}/#ticket/zoom/#{ticket.id}
